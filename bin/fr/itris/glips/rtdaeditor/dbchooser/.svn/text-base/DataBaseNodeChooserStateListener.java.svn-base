package fr.itris.glips.rtdaeditor.dbchooser;

/**
 * the listener to the state of the data base node chooser
 * @author Jordi SUC
 */
public class DataBaseNodeChooserStateListener implements 
	Comparable<DataBaseNodeChooserStateListener>{

	/**
	 * notifies that the selection is correct
	 * @param correct whether the selection is correct
	 */
	public void notifySelectionCorrect(boolean correct){}
	
	public int compareTo(DataBaseNodeChooserStateListener o) {

		return 0;
	}
}
