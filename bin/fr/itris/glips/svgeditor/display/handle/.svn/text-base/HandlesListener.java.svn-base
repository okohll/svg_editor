package fr.itris.glips.svgeditor.display.handle;

import java.util.*;

/**
 * the listener to the handles changes 
 * @author Jordi SUC
 */
public abstract class HandlesListener {

	/**
	 * notifies that the current handle has changed
	 * @param currentHandle the current handle
	 * @param handles the set of the displayed handles
	 */
	public abstract void handleChanged(SVGHandle currentHandle, Set<SVGHandle> handles);
	
	/**
	 * notifies that the current handle content has changed
	 * @param currentHandle the current handle
	 */
	public void handleContentChanged(SVGHandle currentHandle){
		
		
	}
}
