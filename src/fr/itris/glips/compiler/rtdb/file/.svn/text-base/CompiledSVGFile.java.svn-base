package fr.itris.glips.compiler.rtdb.file;

import org.w3c.dom.*;

/**
 * the class of the compiled svg file
 * @author ITRIS, Jordi SUC
 */
public class CompiledSVGFile extends CompiledFile {
	
    /**
     * the constructor of the class
     * @param fileManager the file manager
     * @param path the path of the file
     * @param doc the document of this compiled file
     */
    public CompiledSVGFile(FileManager fileManager, String path, Document doc){
    	
    	super(fileManager, path, doc, SVG_FILE);
    }
    
}
