package fr.itris.glips.compiler.rtdb.file;

import org.w3c.dom.*;

/**
 * the class of the compiled view file
 * @author ITRIS, Jordi SUC
 */
public class CompiledViewsFile extends CompiledFile{

    /**
     * the constructor of the class
     * @param fileManager the file manager
     * @param path the path of the file
     * @param doc the document of this compiled file
     */
    public CompiledViewsFile(FileManager fileManager, String path, Document doc){
        
        super(fileManager, path, doc, XML_FILE);
    }
}
